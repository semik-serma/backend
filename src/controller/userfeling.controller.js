import { userfelingModel } from "../models/userfelingModels.js";
import { errorResponse } from "../utils/response.js";


const likecontroller = async (req, res) => {
try {
    
} catch (error) {
    errorResponse(res,'error at likecontroller',error.message)
}

};
